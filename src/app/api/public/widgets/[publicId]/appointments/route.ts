import { handleAppointment, handleOptions } from "@/lib/server/widget-public-handlers";
export const dynamic = "force-dynamic";
export const POST = handleAppointment;
export const OPTIONS = handleOptions;
